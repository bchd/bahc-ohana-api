%h1 Tag Management

- if current_admin.super_admin?
  = link_to "Add New Tag", new_admin_tag_path

%div
  - if @tags.empty?
    %span
      No results found
  - else
    %p
      - @tags.each do |tag|
        %ul
          = render "tag_list_element", tag: tag
      = paginate @tags
    