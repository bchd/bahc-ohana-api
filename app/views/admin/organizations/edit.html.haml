.content-box
  %h2= @organization.name
  = last_updated_at(@updated)
= form_for [:admin, @organization], html: { class: 'edit-entry' } do |f|
  = render 'admin/organizations/form', f: f, organization: @organization
