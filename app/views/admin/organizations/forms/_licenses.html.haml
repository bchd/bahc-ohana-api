.inst-box
  %header
    %strong
      = f.label :licenses
    %p.desc
      = t('.description')

  = field_set_tag do
    = f.select :licenses, organization.licenses, {}, multiple: true, class: 'form-control', data: { placeholder: t('.placeholder') }
