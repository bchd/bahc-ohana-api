.inst-box
  %header
    = f.label :wait_time
    %p.desc
      = t('.description')

  %p
    = f.select :wait_time, options_for_select(wait_time_select_options, service.wait_time), {include_blank: 'Unknown'}, {:class => "form-control"}
