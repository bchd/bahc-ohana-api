.inst-box
  %header
    %strong
      = f.label :categories
    %p.desc
      = t('.description')

  = hidden_field_tag 'service[category_ids][]', nil
  = field_set_tag nil, id: 'categories' do
    = f.label :services
    = categories_expand_button
    = nested_categories(Category.unarchived)
