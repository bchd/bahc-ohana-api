.inst-box
  %header
    %strong
      = f.label :keywords
    %p.desc
      = t('.description')

  = field_set_tag do
    = f.select :keywords, service.keywords, {}, multiple: true, class: 'form-control', data: { placeholder: t('.placeholder') }

