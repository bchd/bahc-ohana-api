<h1>New Flag Category</h1>

<%= form_for [:admin, @flag_category], url: admin_flag_categories_path, html: { method: :post } do |f| %>
  <%= render 'form', f: f, flag_category: @flag_category %>
<% end %>

<%= link_to 'Back', admin_flag_categories_path %>
