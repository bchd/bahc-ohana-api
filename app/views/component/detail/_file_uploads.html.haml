- if location.file_uploads.present?
  %section.location-sidebar-content.file_upload
    %ul
      - location.file_uploads.each do |upload|
        %li
          %span
            %a{:href => upload.image_url, :target => "_blank"}= "#{upload.image.metadata['filename']}"
